# Welch Statistic Epsilon

The `+ 1e-6` on the pooled-variance standard error guards against zero-variance windows causing division-by-zero. In RPS with stationary opponents, the variance is often genuinely tiny.
